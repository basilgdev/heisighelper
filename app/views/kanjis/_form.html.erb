<%= form_with(model: kanji) do |form| %>
  <% if kanji.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(kanji.errors.count, "error") %> prohibited this kanji from being saved:</h2>

      <ul>
        <% kanji.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :kanji, style: "display: block" %>
    <%= form.text_field :kanji %>
  </div>

  <div>
    <%= form.label :frame_number, style: "display: block" %>
    <%= form.number_field :frame_number %>
  </div>

  <div>
    <%= form.label :radicals, style: "display: block" %>
    <%= form.text_field :radicals %>
  </div>

  <div>
    <%= form.label :rank, style: "display: block" %>
    <%= form.number_field :rank %>
  </div>

    <div>
    <%= form.label :entry, style: "display: block" %>
    <%= form.number_field :entry %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
